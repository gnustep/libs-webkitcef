#
# GNUmakefile - Generated by ProjectCenter
#
ifeq ($(GNUSTEP_MAKEFILES),)
 GNUSTEP_MAKEFILES := $(shell gnustep-config --variable=GNUSTEP_MAKEFILES 2>/dev/null)
  ifeq ($(GNUSTEP_MAKEFILES),)
    $(warning )
    $(warning Unable to obtain GNUSTEP_MAKEFILES setting from gnustep-config!)
    $(warning Perhaps gnustep-make is not properly installed,)
    $(warning so gnustep-config is not in your PATH.)
    $(warning )
    $(warning Your PATH is currently $(PATH))
    $(warning )
  endif
endif
ifeq ($(GNUSTEP_MAKEFILES),)
 $(error You need to set GNUSTEP_MAKEFILES before compiling!)
endif

include $(GNUSTEP_MAKEFILES)/common.make

#
# Application
#
VERSION = 0.1
PACKAGE_NAME = NetStep
APP_NAME = NetStep
NetStep_APPLICATION_ICON = netstep_icon.png


#
# Libraries
#
# NetStep_LIBRARIES_DEPEND_UPON += -lWebKit -lEGL -lGLESv2 -lcef -lvk_swiftshader 

#
# Resource files
#
NetStep_RESOURCE_FILES = \
Resources/NetStep.gorm \
Resources/NetStepWindowController.gorm \
Resources/netstep_icon.png

#
# Header files
#
NetStep_HEADER_FILES = \
AppController.h \
NetStepHTMLDocument.h \
NetStepWindowController.h \
NetStepDocumentController.h

#
# Objective-C Class files
#
NetStep_OBJC_FILES = \
AppController.m \
NetStepHTMLDocument.m \
NetStepWindowController.m \
NetStepDocumentController.m

#
# Other sources
#
NetStep_OBJC_FILES += \
NetStep_main.m 

#
# Makefiles
#
-include GNUmakefile.preamble
include $(GNUSTEP_MAKEFILES)/aggregate.make
include $(GNUSTEP_MAKEFILES)/application.make
-include GNUmakefile.postamble
